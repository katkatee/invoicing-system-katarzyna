package pl.futurecollars.invoicing.config

import spock.lang.Specification

class ConfigTest extends Specification {
    def "dummy test to cover config"() {
        setup:
        def config = new Config()
    }
}
